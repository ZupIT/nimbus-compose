pluginManagement {
    repositories {
        mavenLocal()
        google()
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = "Nimbus Compose"
include(":compose")
include(":compose-sample")
